spring:
  application:
    name: Fisrt-API-REST
  datasource:
    dirver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/first-api-rest?createDatabaseIfNotExist=true
    username: laykon
    password: spring
  jpa:
    hibernate:
      ddl-auto: update
    show.sql: true
  server:
    error:
      include-stacktrace: false